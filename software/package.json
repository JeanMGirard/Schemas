{
  "$schema": "http://jeanmgirard.github.io/schemas/software#",

  "type": "object",
  "definitions": {
    "version": {
      "type": "string",
      "properties": {
        "name": { "type": "string" }
      },
      "required": ["name"]
    }
  },

  "properties": {
    "name":    { "type": "string" },
    "version": { "$ref": "definitions.json#/address" }
  },
  "required": ["name"]
}
